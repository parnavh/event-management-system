generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Event {
  id         String   @id @default(cuid())
  event_name String
  city_name  String
  date       DateTime
  latitude   Float
  longitude  Float
}
